# ─── LLM Provider ─────────────────────────────────────────────────────────────
# LLM_PROVIDER=ollama         # default — llama3.1:8b locally
# LLM_PROVIDER=gemma_ollama   # gemma2:9b locally via Ollama
LLM_PROVIDER=groq             # llama-3.1-8b-instant via Groq cloud (free)
GROQ_API_KEY=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

# ─── Database ──────────────────────────────────────────────────────────────────
# Local (docker-compose postgres — used when running locally)
DATABASE_URL=postgresql+asyncpg://resume_user:resume_password@localhost:5432/resume_maker
# Production (Neon.tech — set this in Render env vars, not here)
# DATABASE_URL=postgresql+asyncpg://username:password@ep-cool-cloud-123.pooler.neon.tech/neondb?sslmode=require

# ─── Auth / JWT ────────────────────────────────────────────────────────────────
# Generate a strong random value for production: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=super_secret_dev_key_do_not_use_in_prod

# ─── CORS ──────────────────────────────────────────────────────────────────────
# Comma-separated list of allowed frontend origins
# Production example: CORS_ORIGINS=https://your-app.vercel.app
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
